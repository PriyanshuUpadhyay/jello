"""jello — one CLI for the claude, codex, and Prime Agent account profiles.

The version is stamped on the first line of the rendered shell-init script, which is how
the cache under ~/.cache/jello knows whether it is current; keep it equal to the version
in pyproject.toml.
"""

__version__ = "0.2.0"
