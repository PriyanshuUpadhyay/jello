.herdr/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# The Swift package's build output; `yelo hud install` rebuilds it on demand.
apps/UsageHUD/.build/

# `uv run` writes this, and yelo has no dependencies for it to pin.
uv.lock

# Local files from the former Herdr install; live scripts still link here.
/assets/*
!/assets/bin
!/assets/claude
